import React, { useEffect } from 'react';
import { useHistory } from 'react-router';
import GithubLoginBtn from '@src/components/GithubLogin';

const Login = () => {
  return <h2>Login page</h2>;
};

export default Login;
